/*#include<stdio.h>
int main()
{
    double s,t,a;
    scanf("#lf",&s)
    scanf("#lf",&t)
    scanf("#lf",&a)
    double d;
    d=s*t+0.5*a*t*t;
    print("%.2lf if meters",d);
    return 0;
}
*/

/*#include <stdio.h>
int main()
{
    int N;
    scaf("%d", &N);
    int da = (N * 10) / 100;
    int ta = (N * 12) / 100;
    int grossSalary = N + da + ta;
    printf("Gross salary = %d", grossSalary);
    return 0;
}
*/
/*#include<stdio.h>
int main()
{
    int a,b,c,d,e;
    scanf("%d %d %d %d %d",%a %b %c %d %e );
    int total= a+b+c+d+e;
    float percentage =(total/500.0)*100;
    printf("Total: %d\n",total);
    printf("Percentage: %.2f",percentage);
    return 0;
}**/


/**
#include<stdio.h>
int main()
{
    int n;
    scanf("%d",&n);
    int a[n];
    for(int i=0;i<n;i++);
        scanf("%d",&a[i]);
    int val;
    scanf("%d",&val);
    n++;
    for(int i=n;i>0;i--);
    {
        a[i]=a[i-1];

    }
    a[0]=val;
    for (int i=0;i<n;i++)
    {
        printf("%d ",a[i]);
    }
    return 0;
}**/

#include<stdio.h>
int main()
{
    int r,c;
    scanf("%d", &r,&c);
    int a[r][c];
    for (int i=0;i<r;i++);
    {
        for(int j=o; k,c;j++);
        {
           scanf("%d",&a[i][j]);
        }
    }
    printf("\n"):

}
return 0